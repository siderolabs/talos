name: hello-host
runnerMode: host
container:
  entrypoint: /usr/local/bin/hello-world
  environment:
    - MODE=host
  args:
    - --log=debug
restart: always
preShutdown:
  entrypoint: /usr/local/bin/hello-shutdown
  args:
    - --graceful
  timeout: 30s
