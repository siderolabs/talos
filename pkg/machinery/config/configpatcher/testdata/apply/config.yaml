version: v1alpha1
machine:
  network:
    hostname: hostname1
    interfaces:
      - interface: eth0
        dhcp: true
cluster:
  proxy:
    extraArgs:
       metrics-bind-address: $(POD_IP):10249
       foo: bar
