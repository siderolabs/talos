apiVersion: v1alpha1
kind: StoragePool
name: vm-images
volume:
  name: vm-data
---
apiVersion: v1alpha1
kind: UserVolumeConfig
name: vm-data
provisioning:
  diskSelector:
    match: disk.transport == "nvme" && !system_disk
  minSize: 10GiB
  maxSize: 100GiB
