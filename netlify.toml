[build]
command = "hugo --gc --minify"
publish = "public"
base  = "website"

[build.environment]
HUGO_VERSION = "0.150.1"
NODE_VERSION = "23.10"

# must match the version in /website/go.mod
GO_VERSION = "1.25.1"
