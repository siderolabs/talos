# THIS FILE WAS AUTOMATICALLY GENERATED BY KRES, PLEASE DO NOT EDIT.
#
# Generated on 2026-09-01T12:13:02Z by kres 95505eb.

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true
'on':
  workflow_run:
    workflows:
      - artifacts-cron
    types:
      - completed
name: integration-extensions-libvirt-triggered
jobs:
  default:
    permissions:
      actions: read
    runs-on:
      group: large
    if: github.event.workflow_run.conclusion == 'success'
    steps:
      - name: gather-system-info
        id: system-info
        uses: kenchan0130/actions-system-info@59699597e84e80085a750998045983daa49274c4 # version: v1.4.0
        continue-on-error: true
      - name: print-system-info
        run: |
          MEMORY_GB=$((${{ steps.system-info.outputs.totalmem }}/1024/1024/1024))

          OUTPUTS=(
            "CPU Core: ${{ steps.system-info.outputs.cpu-core }}"
            "CPU Model: ${{ steps.system-info.outputs.cpu-model }}"
            "Hostname: ${{ steps.system-info.outputs.hostname }}"
            "NodeName: ${NODE_NAME}"
            "Kernel release: ${{ steps.system-info.outputs.kernel-release }}"
            "Kernel version: ${{ steps.system-info.outputs.kernel-version }}"
            "Name: ${{ steps.system-info.outputs.name }}"
            "Platform: ${{ steps.system-info.outputs.platform }}"
            "Release: ${{ steps.system-info.outputs.release }}"
            "Total memory: ${MEMORY_GB} GB"
          )

          for OUTPUT in "${OUTPUTS[@]}";do
            echo "${OUTPUT}"
          done
        continue-on-error: true
      - name: checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # version: v7.0.0
      - name: Unshallow
        run: |
          git fetch --prune --unshallow
      - name: Set up Docker Buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # version: v4.2.0
        with:
          driver: remote
          endpoint: tcp://buildkit-amd64.ci.svc.cluster.local:1234
        timeout-minutes: 10
      - name: Download artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # version: v8.0.1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          name: talos-artifacts
          path: _out
          run-id: ${{ github.event.workflow_run.id }}
      - name: Fix artifact permissions
        run: |
          xargs -a _out/executable-artifacts -I {} chmod +x {}
      - name: ci-temp-release-tag
        run: |
          make ci-temp-release-tag
      - name: checkout extensions
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # version: v7.0.0
        with:
          path: _out/extensions
          ref: main
          repository: siderolabs/extensions
      - name: set variables
        run: |
          cat _out/talos-metadata >> "$GITHUB_ENV"
      - name: build extensions
        env:
          PLATFORM: linux/amd64
          PUSH: "true"
          REGISTRY: registry.dev.siderolabs.io
        run: |
          make libvirtd extensions-metadata -C _out/extensions
      - name: installer extensions
        env:
          EXTENSIONS_FILTER_COMMAND: grep -E '/libvirtd:'
          IMAGE_REGISTRY: registry.dev.siderolabs.io
        run: |
          make installer-with-extensions
      - name: e2e-extensions-libvirt
        env:
          EXTRA_TEST_ARGS: -talos.extensions.qemu -talos.extensions.libvirt
          GITHUB_STEP_NAME: ${{ github.job}}-e2e-extensions-libvirt
          IMAGE_REGISTRY: registry.dev.siderolabs.io
          INTEGRATION_TEST_RUN: TestIntegration/api.ExtensionsSuiteQEMU/TestExtensionsLibvirt
          QEMU_MEMORY_WORKERS: "4096"
          QEMU_WORKERS: "1"
          SHORT_INTEGRATION_TEST: 'yes'
          WITH_CONFIG_PATCH_WORKER: '@_out/installer-extensions-patch.yaml:@hack/test/patches/extensions-libvirt.yaml'
        run: |
          sudo -E make e2e-qemu
      - name: save artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # version: v7.0.1
        with:
          name: talos-logs-integration-extensions-libvirt
          path: |-
            /tmp/logs-*.tar.gz
            /tmp/support-*.zip
          retention-days: "5"
