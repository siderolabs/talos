apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: trident-ontap-san
provisioner: csi.trident.netapp.io
parameters:
  backendType: "ontap-san"
  fsType: "ext4"
  selector: "protocol=iscsi"
allowVolumeExpansion: true
reclaimPolicy: Delete
volumeBindingMode: Immediate
