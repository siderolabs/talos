apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: trident-ontap-nas
provisioner: csi.trident.netapp.io
parameters:
  backendType: "ontap-nas"
  fsType: "nfs"
  selector: "protocol=nas"
allowVolumeExpansion: true
reclaimPolicy: Delete
volumeBindingMode: Immediate
